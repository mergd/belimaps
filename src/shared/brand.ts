/**
 * Beli brand tokens pulled from https://beliapp.com/ (Squarespace site.css / custom.css).
 * Accent teal #134f5c appears on brand blocks; wordmark uses Playfair Display; UI sans is Helvetica Neue.
 */
export const BELI = {
  teal: "#134f5c",
  ink: "#000000",
  paper: "#ffffff",
  muted: "#666666",
  line: "#e6e6e6",
  soft: "#f5f5f5",
} as const;
